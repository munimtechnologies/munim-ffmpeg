name: Build iOS

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ios-build.yml"
      - "package.json"
      - "package-lock.json"
      - "example/**"
      - "packages/munim-ffmpeg/ios/**"
      - "packages/munim-ffmpeg/nitrogen/generated/ios/**"
      - "packages/munim-ffmpeg/nitrogen/generated/shared/**"
      - "packages/munim-ffmpeg/src/**"
      - "packages/munim-ffmpeg/*.podspec"
      - "packages/munim-ffmpeg/nitro.json"
      - "packages/munim-ffmpeg/react-native.config.js"
  pull_request:
    paths:
      - ".github/workflows/ios-build.yml"
      - "package.json"
      - "package-lock.json"
      - "example/**"
      - "packages/munim-ffmpeg/ios/**"
      - "packages/munim-ffmpeg/nitrogen/generated/ios/**"
      - "packages/munim-ffmpeg/nitrogen/generated/shared/**"
      - "packages/munim-ffmpeg/src/**"
      - "packages/munim-ffmpeg/*.podspec"
      - "packages/munim-ffmpeg/nitro.json"
      - "packages/munim-ffmpeg/react-native.config.js"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build iOS example
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate iOS project
        run: npx expo prebuild --platform ios --clean --no-install
        working-directory: example

      - name: Install CocoaPods dependencies
        run: pod install
        working-directory: example/ios

      - name: Build simulator app
        run: |
          set -o pipefail
          xcodebuild \
            -workspace MunimFFmpegExample.xcworkspace \
            -scheme MunimFFmpegExample \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build
        working-directory: example/ios
